spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
logging:
  level:
    org:
      springframework:
        test:
          context:
            cache: DEBUG
    de:
      schulung:
        spring:
          customers:
            shared:
              interceptors: DEBUG
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  liquibase:
    enabled: false
logging:
  level:
    org:
      springframework:
        test:
          context:
            cache: DEBUG
    de:
      schulung:
        spring:
          customers:
            shared:
              interceptors: DEBUG